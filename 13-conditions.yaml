- name: check number of live sessions
  hosts: local
  connection: local
  vars:
    live_sessions: 6
  tasks:
    - name: is ansible more than 5 sessions
      debug:
        msg: "Yes, Ansible is more than 5 sessions"
      when: live sessions > 5